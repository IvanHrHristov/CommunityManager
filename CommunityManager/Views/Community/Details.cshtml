@model CommunityDetailsViewModel

@{
    ViewBag.Title = "Manage a community";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="d-flex justify-content-center">
    <div class="card " style="width: 50rem;">
        <div class="card-body">
            <h3 class="card-title text-center">@Model.Name</h3>
            <p class="card-text"><span class="fw-bold">Description: </span>@Model.Description</p>
            <p class="card-text"><span class="fw-bold">Created on: </span>@Model.CreatedOn.ToShortDateString()</p>
        </div>

        <div class="mb-3 d-flex justify-content-center">
            <a asp-controller="Community" asp-action="Manage" asp-route-id="@Model.Id" class="btn btn-primary mb-2">Manage</a>
            <span></span>
            <a asp-controller="Marketplace" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger mb-2">Delete</a>
        </div>

        @*<div class="mb-3 d-flex justify-content-center">
            @if (this.User?.Identity != null && this.User.Identity.Name != Model.Seller)
            {
                <a asp-controller="Marketplace" asp-action="Buy" asp-route-id="@Model.Id" class="btn btn-success mb-2">Buy</a>
            }

            @if (this.User?.Identity != null && this.User.Identity.Name == Model.Seller)
            {
                <a asp-controller="Marketplace" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary mb-2">Edit</a>
                <span></span>
                <a asp-controller="Marketplace" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger mb-2">Delete</a>
            }
        </div>*@
    </div>
</div>

<nav id="sidebarMenu"
     class="collapse d-lg-block sidebar collapse bg-white">
    <div class="position-sticky">
        <div class="list-group list-group-flush mx-3 mt-4">
            <p class="list-group-item">MARKETPLACE</p>

            @foreach (var marketplace in Model.Marketplaces)
            {
                <a asp-controller="Marketplace" asp-action="All" asp-route-id="@marketplace.Id" asp-route-param="@Model.Id"
               class="list-group-item list-group-item-action py-2 ripple"
               aria-current="true">
                    <span><b>@marketplace.Name</b></span>
                    <div> All products</div>
                </a>
            }

            @if (Model.CreatorId == ViewBag.currentUserId)
            {
                <a asp-controller="Community" asp-action="Add" asp-route-id="@((Guid)Model.Id)"
               class="list-group-item list-group-item-action py-2 ripple"
               aria-current="true">
                    <span>Create a new marketplace</span>
                </a>
            }

            @*<a asp-controller="Marketplace" asp-action="Sell"
            class="list-group-item list-group-item-action py-2 ripple"
            aria-current="true">
            <span>Sell a product</span>
            </a>*@

            @*<a href="#"
            class="list-group-item list-group-item-action py-2 ripple active">
            <i class="fas fa-chart-area fa-fw me-3"></i><span>Webiste traffic</span>
            </a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple"><i class="fas fa-lock fa-fw me-3"></i><span>Password</span></a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple">
            <i class="fas fa-chart-line fa-fw me-3"></i><span>Analytics</span>
            </a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple">
            <i class="fas fa-chart-pie fa-fw me-3"></i><span>SEO</span>
            </a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple"><i class="fas fa-chart-bar fa-fw me-3"></i><span>Orders</span></a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple">
            <i class="fas fa-globe fa-fw me-3"></i><span>International</span>
            </a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple">
            <i class="fas fa-building fa-fw me-3"></i><span>Partners</span>
            </a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple">
            <i class="fas fa-calendar fa-fw me-3"></i><span>Calendar</span>
            </a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple"><i class="fas fa-users fa-fw me-3"></i><span>Users</span></a>
            <a href="#"
            class="list-group-item list-group-item-action py-2 ripple"><i class="fas fa-money-bill fa-fw me-3"></i><span>Sales</span></a>*@
        </div>
    </div>
</nav>

@section css {
    <link rel="stylesheet" href="~/css/sidebar.css" />
}